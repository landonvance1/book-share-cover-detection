services:
  cover-detection:
    build: .
    expose:
      - "8000"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - booksharing

networks:
  booksharing:
    external: true
