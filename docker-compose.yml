services:
  cover-detection:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - booksharing

networks:
  booksharing:
    external: true
